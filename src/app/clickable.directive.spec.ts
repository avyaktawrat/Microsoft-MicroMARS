import { ClickableDirective } from './clickable.directive';

describe('ClickableDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickableDirective();
    expect(directive).toBeTruthy();
  });
});
